<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sarvgya98.grp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="GRP"
        android:roundIcon="@mipmap/grp_app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UnderDevelopment" />
        <activity android:name=".ResponderForm" />
        <activity android:name=".CaseTimeline" />
        <activity android:name=".Staff"></activity>
    </application>

</manifest>